import React from "react";
import { DigitCRUD } from "@cthit/react-digit-components";

const Reservations = () => {
    return <DigitCRUD name={"reservations"} path={"/reservations"} />;
};

export default Reservations;
