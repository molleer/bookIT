<% @reports.each do |report| %>
<%= report.title %>
<%= report.group %>
<%= report.party_responsible_name %>
<%= report.booking.booking_range %>
<% unless report == @reports.last %>---------<% end %>
<% end %>
