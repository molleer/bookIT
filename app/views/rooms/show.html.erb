<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>

<p>
  <strong>Festrum:</strong>
  <%= @room.allow_party ? 'Ja!' : 'Nej.' %>
</p>

<% if can? :manage, Room %>
	<%= link_to 'Edit', edit_room_path(@room) %> |
<% end %>
<%= link_to 'Back', rooms_path %>
